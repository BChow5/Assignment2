#!/bin/bash

ln -s copy_config/bin $HOME/bin
ln -s copy_config/config $HOME/.config
ln -s copy_config/home/bashrc $HOME/.bashrc

# creating symbolic links from our cloned configuration files to our files on the PC for the new user
